package com.markvtls.weatherapp.domain.model

/**
 * Data class for storing LocationManager result as single object.
 */
data class Coordinates (
    val latitude: Double,
    val longitude: Double
                    )